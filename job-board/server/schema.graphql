type Query {
    jobs: [Job!]
}

# just a regular comment

"""
Represents a job!
"""

type Job {
    id: ID!
    title: String!
    description: String
    date: String!
    company: Company
}

type Company {
    id: ID!
    name: String!
    description: String
}